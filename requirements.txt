# Vision & facial analysis
opencv-python>=4.9
mediapipe>=0.10

# HTTP / API clients
requests>=2.31
httpx>=0.27          # async-capable alternative if needed
openai>=1.0          # OpenAI-compatible client for NVIDIA Nemotron endpoints

# Config & environment
python-dotenv>=1.0
pyyaml>=6.0

# Data modeling (shared interface contracts)
dataclasses-json>=0.6   # optional: JSON serialization for dataclasses

# Agent framework (placeholder — replace with actual OpenClaw package name)
# openclaw>=0.1

# Storage
# SQLite is stdlib; uncomment below if switching to Redis
# redis>=5.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
